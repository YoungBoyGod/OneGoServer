# 🛑 如何触发服务器关闭 - 演示指南

## 📋 当前运行的进程
```
air进程：PID 25833
main进程：PID 26433
```

## 🎯 触发关闭的4种方法

### 1. **最常用：Ctrl+C** ⭐️
在运行air的终端中按：
```bash
Ctrl + C
```
- 发送 `SIGINT` 信号（信号值：2）
- 最直接、最常用的方式
- 适合开发环境

### 2. **使用kill命令（默认SIGTERM）**
```bash
kill 26433  # 关闭main进程
# 或
kill 25833  # 关闭air进程（会同时关闭main）
```
- 发送 `SIGTERM` 信号（信号值：15）
- 适合脚本和自动化

### 3. **明确指定SIGTERM信号**
```bash
kill -15 26433  # -15表示SIGTERM
kill -TERM 26433  # 使用信号名称
```

### 4. **明确指定SIGINT信号**
```bash
kill -2 26433   # -2表示SIGINT
kill -INT 26433 # 使用信号名称
```

## 🚨 不要使用的命令
```bash
kill -9 26433   # SIGKILL信号
kill -KILL 26433
```
- 强制杀死进程，**无法被捕获**
- 不会触发优雅关闭
- 可能导致数据丢失

## 🔍 代码中的信号处理
```go
signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM) // 只监听这两种信号
sig := <-quit                                        // 等待信号
log.Printf("Server is shutting down...:%v", sig)     // 打印收到的信号
```

## 📝 关闭时会看到的日志
```
Server is shutting down...:interrupt    # Ctrl+C (SIGINT)
Server is shutting down...:terminated   # kill命令 (SIGTERM)
Server exited
Server time: 2025-06-19T10:47:30+08:00
```

## 🧪 实际演示

### 演示1：使用Ctrl+C
1. 在air运行的终端按 `Ctrl+C`
2. 观察优雅关闭过程
3. 看到完整的关闭日志

### 演示2：使用kill命令
1. 查找进程ID：`ps aux | grep main`
2. 执行：`kill [PID]`
3. 观察相同的优雅关闭效果

## 🔄 优雅关闭流程详解

### 步骤1：收到信号
```
signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM)
```

### 步骤2：开始关闭
```
log.Printf("Server is shutting down...:%v", sig)
```

### 步骤3：创建超时上下文
```go
ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
```

### 步骤4：优雅关闭服务器
```go
srv.Shutdown(ctx)  // 等待现有请求完成，最多5秒
```

### 步骤5：完成退出
```
log.Println("Server exited")
log.Println("Server time:", time.Now().Format(time.RFC3339))
```

## ⏱️ 时间轴分析

| 时间点 | 事件 | 说明 |
|--------|------|------|
| T+0s | 收到信号 | 开始关闭流程 |
| T+0s | 停止接受新请求 | HTTP服务器不再接受新连接 |
| T+0-5s | 等待现有请求 | 让正在处理的请求完成 |
| T+5s | 强制关闭 | 如果超时，强制终止 |
| T+5s+ | 程序退出 | 释放所有资源 |

## 🎯 选择合适的关闭方式

### 开发环境
- ✅ 使用 `Ctrl+C`
- ✅ 简单直接
- ✅ 即时反馈

### 生产环境
- ✅ 使用 `kill PID`（SIGTERM）
- ✅ 脚本友好
- ✅ 标准做法

### 容器环境
- ✅ Docker会发送SIGTERM
- ✅ Kubernetes使用SIGTERM
- ✅ 符合容器标准

## 🚀 热重载环境特殊说明

在使用air时：
- 关闭air会同时关闭main进程
- air会自动重启main进程（如果是文件变化）
- 手动关闭main进程，air会重新启动它

## 创建时间
${new Date().toLocaleString('zh-CN')} 