# OneGoServer002 下一步开发计划

## 🎯 当前状态总结

### ✅ 已完成功能

1. **数据库层面**
   - ✅ 数据库迁移系统完整实现
   - ✅ 所有迁移文件（001-004）成功执行
   - ✅ 4个核心表创建完成：tasks、devices、task_assignment_queue、device_task_queue

2. **业务模型层面**
   - ✅ Task模型完整定义（包含JSONB、状态常量、请求响应结构）
   - ✅ TaskRepository完整实现（CRUD + 过滤 + 分页 + 统计）
   - ✅ TaskService完整实现（14个业务方法）
   - ✅ TaskController完整实现（14个API端点）

3. **系统基础组件**
   - ✅ 日志系统（Zap）
   - ✅ 配置管理（Viper）
   - ✅ 数据库连接（GORM + PostgreSQL）
   - ✅ 缓存系统（Redis）
   - ✅ 消息队列（Kafka）
   - ✅ Web框架（Gin）

### 🔧 技术架构现状

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Controller    │    │     Service     │    │   Repository    │
│   (HTTP API)    │───▶│  (业务逻辑)     │───▶│    (数据访问)    │
│                 │    │                 │    │                 │
│ ✅ TaskController│    │ ✅ TaskService   │    │ ✅ TaskRepository│
│ ❌ DeviceController│    │ ❌ DeviceService │    │ ⚠️ DeviceRepository│
│                 │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│     Router      │    │     Models      │    │    Database     │
│   (路由配置)     │    │   (数据结构)     │    │   (PostgreSQL)  │
│                 │    │                 │    │                 │
│ ✅ Task Routes   │    │ ✅ Task Models   │    │ ✅ 4 Tables      │
│ ❌ Device Routes │    │ ⚠️ Device Models │    │ ✅ Migrations    │
│                 │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

---

## 📋 下一步开发优先级

### 🔥 第一优先级：系统集成测试（预计2小时）

#### 1.1 修复导入路径问题
当前存在模块导入路径不一致的问题：
```bash
# 需要统一所有文件的导入路径为：
github.com/YoungBoyGod/OneGoServer/internal/...
```

**待修复文件：**
- `internal/data/device_repository.go` - Device模型导入问题
- `internal/biz/device/queue_models.go` - Task模型引用问题
- 可能的其他导入路径不一致文件

#### 1.2 创建系统初始化器
```go
// internal/bootstrap/app.go
type Application struct {
    DB             *gorm.DB
    TaskRepo       data.TaskRepository
    TaskService    service.TaskService
    TaskController *controller.TaskController
    Router         *gin.Engine
}

func NewApplication() *Application {
    // 1. 初始化数据库
    // 2. 初始化Repository
    // 3. 初始化Service
    // 4. 初始化Controller
    // 5. 设置路由
    // 6. 返回完整应用
}
```

#### 1.3 更新main.go集成所有组件
```go
// main.go 需要更新为：
func main() {
    // 1. 加载配置
    // 2. 初始化基础组件（日志、数据库等）
    // 3. 运行数据库迁移
    // 4. 创建Application实例
    // 5. 启动HTTP服务器
    // 6. 优雅关闭处理
}
```

### 🚀 第二优先级：API功能验证（预计3小时）

#### 2.1 创建API测试脚本
```bash
# 创建任务
curl -X POST http://localhost:8080/api/v1/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试任务",
    "type": "backup",
    "priority": 5,
    "description": "这是一个测试任务"
  }'

# 获取任务列表
curl -X GET "http://localhost:8080/api/v1/tasks?page=1&size=10"

# 执行任务
curl -X POST http://localhost:8080/api/v1/tasks/1/execute

# 获取任务状态
curl -X GET http://localhost:8080/api/v1/tasks/1/status
```

#### 2.2 验证所有14个Task API端点
- ✅ POST /api/v1/tasks - 创建任务
- ✅ GET /api/v1/tasks - 获取任务列表  
- ✅ GET /api/v1/tasks/:id - 获取任务详情
- ✅ PUT /api/v1/tasks/:id - 更新任务
- ✅ DELETE /api/v1/tasks/:id - 删除任务
- ✅ GET /api/v1/tasks/:id/status - 获取任务状态
- ✅ PUT /api/v1/tasks/:id/status - 修改任务状态
- ✅ POST /api/v1/tasks/:id/execute - 执行任务
- ✅ POST /api/v1/tasks/:id/cancel - 取消任务
- ✅ POST /api/v1/tasks/:id/dispatch - 分发任务
- ✅ GET /api/v1/tasks/query - 查询任务
- ✅ GET /api/v1/tasks/:id/detail - 获取任务详情
- ✅ GET /api/v1/tasks/:id/result - 获取任务结果
- ✅ GET /api/v1/tasks/:id/execution - 获取执行记录
- ✅ GET /api/v1/tasks/:id/stats - 获取任务统计

#### 2.3 创建健康检查端点测试
```bash
# 健康检查
curl http://localhost:8080/health

# 简单ping
curl http://localhost:8080/ping
```

---

## 🏗️ 第三优先级：Device模块实现（预计6小时）

### 3.1 修复Device模型问题（1小时）
当前Device相关文件存在编译错误，需要：
1. 修复`internal/biz/device/models.go`中的类型定义
2. 确保Device模型与数据库表结构一致
3. 实现Device相关的请求响应结构

### 3.2 实现DeviceService（2小时）
```go
type DeviceService interface {
    // 基础CRUD
    RegisterDevice(ctx context.Context, req *device.DeviceCreateRequest) (*device.Device, error)
    GetDevice(ctx context.Context, id int64) (*device.Device, error)
    UpdateDevice(ctx context.Context, id int64, req *device.DeviceUpdateRequest) error
    DeleteDevice(ctx context.Context, id int64) error
    ListDevices(ctx context.Context, filter *device.DeviceFilter) (*device.DeviceListResponse, error)
    
    // 状态管理
    DeviceOnline(ctx context.Context, deviceID string) error
    DeviceOffline(ctx context.Context, deviceID string) error
    GetDeviceStatus(ctx context.Context, deviceID string) (*device.DeviceStatus, error)
    
    // 心跳和命令
    UpdateDeviceHeartbeat(ctx context.Context, deviceID string, heartbeat *device.HeartbeatData) error
    SendCommand(ctx context.Context, deviceID string, command *device.Command) error
    GetDeviceLogs(ctx context.Context, deviceID string, filter *device.LogFilter) ([]device.DeviceLog, error)
}
```

### 3.3 完善DeviceController（2小时）
更新`internal/controller/device.go`，将所有TODO占位实现替换为真实的业务逻辑调用。

### 3.4 集成Device路由（1小时）
在`internal/router/router.go`中确保Device相关路由正确注册。

---

## 🔄 第四优先级：任务分配系统（预计4小时）

### 4.1 任务队列管理
基于已创建的`task_assignment_queue`表实现：
```go
type QueueService interface {
    EnqueueTask(ctx context.Context, taskID int64, requirements *QueueRequirements) error
    DequeueTask(ctx context.Context, deviceID int64) (*AssignedTask, error)
    UpdateQueuePosition(ctx context.Context, taskID int64, position int) error
    GetQueueStatus(ctx context.Context) (*QueueStatistics, error)
}
```

### 4.2 设备负载监控
基于已创建的`device_task_queue`表实现：
```go
type LoadMonitorService interface {
    UpdateDeviceLoad(ctx context.Context, deviceID int64, metrics *LoadMetrics) error
    GetDeviceLoad(ctx context.Context, deviceID int64) (*DeviceLoad, error)
    SelectOptimalDevice(ctx context.Context, requirements *TaskRequirements) (*device.Device, error)
}
```

---

## 🧪 第五优先级：单元测试（预计4小时）

### 5.1 Repository层测试
```go
func TestTaskRepository_Create(t *testing.T) {
    // 数据库集成测试
}

func TestTaskRepository_List(t *testing.T) {
    // 过滤和分页测试
}
```

### 5.2 Service层测试
```go
func TestTaskService_CreateTask(t *testing.T) {
    // 业务逻辑测试
}

func TestTaskService_ExecuteTask(t *testing.T) {
    // 状态转换测试
}
```

### 5.3 Controller层测试
```go
func TestTaskController_CreateTask(t *testing.T) {
    // HTTP API测试
}
```

---

## 📊 进度追踪表

| 功能模块 | 状态 | 完成度 | 预计时间 | 优先级 |
|----------|------|--------|----------|--------|
| Task CRUD API | ✅ 完成 | 100% | - | - |
| 系统集成测试 | 🔄 进行中 | 0% | 2小时 | 🔥 最高 |
| API功能验证 | ⏳ 待开始 | 0% | 3小时 | 🚀 高 |
| Device模块实现 | ⏳ 待开始 | 20% | 6小时 | 🏗️ 中 |
| 任务分配系统 | ⏳ 待开始 | 10% | 4小时 | 🔄 中 |
| 单元测试 | ⏳ 待开始 | 0% | 4小时 | 🧪 低 |
| **总计** | - | **65%** | **19小时** | - |

---

## 🚀 立即行动计划

### 今天就可以开始：

1. **修复导入路径问题**
   ```bash
   # 检查所有Go文件的导入路径
   find . -name "*.go" -exec grep -l "OneGoServer002\|OneGo/" {} \;
   
   # 批量替换导入路径
   find . -name "*.go" -exec sed -i 's|OneGoServer002/|github.com/YoungBoyGod/OneGoServer/|g' {} \;
   ```

2. **创建Application启动器**
   ```bash
   mkdir -p internal/bootstrap
   touch internal/bootstrap/app.go
   ```

3. **测试当前Task API**
   ```bash
   # 启动服务器
   go run main.go
   
   # 测试健康检查
   curl http://localhost:8080/health
   ```

### 成功标准：

- [ ] 项目能够正常编译（`go build`成功）
- [ ] 服务器能够正常启动（8080端口监听）
- [ ] 健康检查端点返回正常响应
- [ ] 至少3个Task API端点功能正常
- [ ] 数据库连接和基础CRUD操作正常

---

## 💡 技术建议

1. **日志记录**：在每个API调用中添加详细的日志记录，便于调试
2. **错误处理**：统一错误响应格式，包含错误码和详细信息  
3. **参数验证**：使用Gin的binding机制进行参数验证
4. **性能监控**：添加API响应时间统计
5. **文档生成**：使用Swagger自动生成API文档

---

## 📈 长期规划（未来2-4周）

1. **认证授权系统**：JWT Token + 权限控制
2. **API限流和熔断**：防止系统过载
3. **消息队列集成**：Kafka异步任务处理
4. **监控和告警**：Prometheus + Grafana
5. **Docker容器化**：生产环境部署
6. **CI/CD流水线**：自动化测试和部署

完成当前计划后，OneGoServer002将成为一个功能完整、架构清晰的任务管理系统！ 